# Copy this file to `.env.local` and replace the placeholder values.
DATABASE_URL=postgresql://postgres:password@localhost:5432/resume

# Enable this for PostgreSQL providers that require TLS.
DATABASE_SSL=false
# Set to false only when your provider requires TLS without a verifiable certificate.
DATABASE_SSL_REJECT_UNAUTHORIZED=true
